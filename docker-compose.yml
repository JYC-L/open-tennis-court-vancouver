version: "3.9"
services:
  frontend:
    build: ./frontend
    platform: linux/amd64
    ports:
      - "8080:80"
    depends_on:
      - backend
  backend:
    build: ./backend
    platform: linux/amd64
    ports:
      - "4325:4325"
